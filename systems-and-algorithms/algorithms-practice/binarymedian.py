#def binarySearch(arr, x, low, high):
#           if low > high:
 #              return False 
 #  
 #          else:
 #              mid = (low + high) / 2 
  #             if x == arr[mid]:
   #                return mid
       
   #            else if x > arr[mid]        // x is on the right side
   #                return binarySearch(arr, x, mid + 1, high)
   #            
   #            else                        // x is on the left side
   #                return binarySearch(arr, x, low, mid - 1) 
   
#def binarymedian(a,b):

a=[1,3,5,7,9]
b=[2,4,6,8,10]
#a=[1,2,3,4,5]
#b=[6,7,8,9,10]
print("The first list : " + str(a))
print("The second list : " + str(a))
   